
<div class="hero-bg">
    <div class="row">
      <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12 signup-cta">
        <div class="signup-title center">
          Sign up to schedule your first trip.
        </div>
        <div class="form-inline col-lg-9 col-lg-offset-3 col-md-9 col-md-offset-3 col-sm-maxwidth col-xs-maxwidth">
        <%= form_for(@user) do |f| %>
          <% if @user.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>
              <ul>
              <% @user.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
              </ul>
            </div>
          <% end %>
            <%= f.email_field :email, autofocus: true, class:"input-group input-group-lg", placeholder:"Enter Email"  %>
            <%= hidden_field_tag 'ga_client_id', '', :class => 'ga-client-id' %>
            <%= f.submit "Get Early Access", class:"btn btn-signup btn-lg" %>
        <% end %>
        </div>
        <div class="col-lg-12 col-md-12 col-sm-12 center instructions">(Don't worry, we don't spam.)   </div>
        </div>
      </div>
    </div>
</div>


<!-- %= render "devise/shared/links" %> -->
